package controller

// BookingController registers shared booking-related routes not owned by
// UserController (guest bookings) or AdminController (reassignment).
// Currently all booking endpoints live in those role-specific controllers;
// this file is a structural placeholder matching the spec's directory layout.
